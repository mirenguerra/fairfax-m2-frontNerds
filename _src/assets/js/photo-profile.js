// Al pulasr el botón de "add picture" se tiene que abrir el explorador para poder elegir la foto. Una vez que se elija aparecerá en la miniatura y en el preview.

// Ponemos input type: file en el HTML
// Escondemos el input Type: file con SCSS

// Recoger input Type:file
// Recoger botón profile-picture
// Recoger photo del preview (añadir clase)
// Recoger input Profile-miniature

// Escuchamos el click del botón Add Picture
// Ponemos handler al click del botón Add Picture
    // Cuando haga click en el botón Add Picture centro el focus en el input type:file ( y esto abre el explorador)
    // Cuando el input type:file cambie tengo que escuchar el cambio (change)
    // Recojo imagen con input.value
    // Pongo la imagen en photo preview y en la Profile miniature